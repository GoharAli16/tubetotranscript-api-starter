# Optional: API requests work without a key at the public rate limit.
# Add a key to use your account's API rate limit. Never expose this value in browser code.
TUBETOTRANSCRIPT_API_KEY=ttt_live_your_key_here

PORT=3001
